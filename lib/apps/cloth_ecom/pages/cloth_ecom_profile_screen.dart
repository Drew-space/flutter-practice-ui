import 'package:flutter/material.dart';

class ClothEcomProfileScreen extends StatelessWidget {
  const ClothEcomProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Cloth Ecom Profile Screen")),
    );
  }
}
