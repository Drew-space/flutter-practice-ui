import 'package:flutter/material.dart';

class FurnitureProfileScreen extends StatelessWidget {
  const FurnitureProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Furniture Profile')));
  }
}
