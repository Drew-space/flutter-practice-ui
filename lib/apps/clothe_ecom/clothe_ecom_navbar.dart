import 'package:flutter/material.dart';

class ClotheEcomNavbar extends StatelessWidget {
  const ClotheEcomNavbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
