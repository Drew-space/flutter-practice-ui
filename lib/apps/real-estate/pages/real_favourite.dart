import 'package:flutter/material.dart';

class RealFavourite extends StatelessWidget {
  const RealFavourite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.white);
  }
}
