import 'package:flutter/material.dart';

class MovieProfileScreen extends StatelessWidget {
  const MovieProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      body: Center(
        child: const Text('Profile information will appear here.'),
      ),
    );
  }
}
